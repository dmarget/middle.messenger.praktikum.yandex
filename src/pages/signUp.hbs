---
head: Регистрация
description: Регистрация
---

<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0"
    />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <link href="../styles/styles.scss" rel="stylesheet" />
    <title>{{head}}</title>
  </head>
  <body>

    <main class="page">
      <section class="page__sign-up">
        <h1 class="title">{{description}}</h1>
        <div class="content">
            <form name="signUpForm">
              {{>input inputClass="input input_fullWidth content__input" inputLabel="Почта" inputType="text" inputName="email"}}
              {{>input inputClass="input input_fullWidth content__input" inputLabel="Логин" inputType="text" inputName="login"}}
              {{>input inputClass="input input_fullWidth content__input" inputLabel="Имя" inputType="text" inputName="first_name"}}
              {{>input inputClass="input input_fullWidth content__input" inputLabel="Фамилия" inputType="text" inputName="second_name"}}
              {{>input inputClass="input input_fullWidth content__input" inputLabel="Телефон" inputType="text" inputName="phone"}}
              {{>input inputClass="input input_fullWidth content__input" inputLabel="Пароль" inputType="password" inputName="password"}}
              {{>input inputClass="input input_fullWidth content__input" inputLabel="Пароль(еще раз)" inputType="password" inputName="oldPassword"}}
              {{> button buttonText="Зарегистрироваться" buttonClass="button button_fullWidth" type="submit"}}
            </form>  
        </div>   
      </section>
    </main>

  </body>
</html>
